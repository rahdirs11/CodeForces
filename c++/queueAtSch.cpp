/*
Queue problem
Boys and girls are standing in a queue in front of the canteen
Rule is girls should be in front of all boys
Time t is just the number of girls to be in front of each boy
*/
#include <iostream>
#include <string>
using namespce std;

int main() {
    int n{}, t{};
    string queue{};
    cin >> n >> t;
    getline(cin, queue);
    
    return 0;
}
